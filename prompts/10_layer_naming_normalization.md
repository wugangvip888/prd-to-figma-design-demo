# 10 Layer Naming Normalization

## 任务

对目标 Figma 范围执行 Frame / 图层命名规范化，并执行 Layer Naming Harness Gate。

本流程只允许修改节点名称，不允许改变视觉、布局、层级、样式、文字内容或业务语义。

## 执行前请读取

```text
docs/process_rules.md
docs/workflow.md
execution/10_layer_naming_normalization.md
rules/wireframe_rules.md
rules/hifi_generation_rules.md
rules/harness_rules.md
workspace/design_system/manual-hifi-adjustment-rules.md（如存在）
最近一次相关 workspace/records/run_xxx.md（如存在）
```

## 放行要求

```text
必须通过 Layer Naming Harness Gate。
未通过时，回到 execution/10_layer_naming_normalization.md 定位问题、修正命名并重新执行 Gate。
未通过 Layer Naming Harness Gate，不得进入 Auto Layout 回填。
```
